---
title: Local Minima in Quadratic-Penalty Relaxations of Binary Linear Programs
title_zh: 二元线性规划的二次惩罚松弛中的局部极小点
authors: "Cheng-Han Huang, Yongliang Sun, Chaoyan Huang, Ismail Alkhouri, Rongrong Wang"
date: 2026-06-27
pdf: "https://arxiv.org/pdf/2606.28734v1"
tags: ["query:boundary-opt"]
score: 10.0
evidence: 确保连续松弛的局部极小值位于盒约束边界上，产生可行的二元解
tldr: "组合优化问题常转化为二次无约束二元规划并松弛到[0,1]^n箱形，通过梯度方法求解，但非凸地形易产生虚假局部最优。本文建立二次惩罚项的充分结构条件，确保松弛问题的每个局部极小点都是二元可行解。对于露天采矿、零一背包和旅行商等经典问题，分析现有公式的缺陷，提出满足条件的新松弛，使投影梯度下降和Adam等算法能安全获得有效二元解。该工作明确了可微优化何时能可靠求解组合二次目标。"
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.28734v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 863, \"height\": 164, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28734v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 860, \"height\": 252, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28734v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 857, \"height\": 263, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28734v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 872, \"height\": 277, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.28734v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 860, \"height\": 668, \"label\": \"Table\"}]"
motivation: 当前梯度方法求解松弛QUBO时，易陷入非二元的虚假局部最优，缺乏可靠性保证。
method: 为二次惩罚项建立充分结构条件，保证箱型松弛问题局部极小点二元可行；对具体问题设计满足条件的替代QUBO。
result: 在露天采矿、0-1背包和旅行商等经典问题上，新松弛使梯度优化（如投影梯度下降和Adam）能安全获得有效二元解。
conclusion: 明确了可微优化方法在二次组合目标上可靠求解的适用范围，为梯度方法求解离散问题提供理论指导。
---

## 摘要
许多组合优化问题存在二次无约束二值表述（QUBO），这些表述通常可松弛到单位超立方体 $[0,1]^n$ 上，并利用可扩展的梯度方法进行优化。然而，由此产生的非凸优化曲面常包含虚假或不可行的局部最优解。本文中，我们对二次惩罚项建立了充分的结构条件，以排除这类失效情况，从而保证松弛问题的每个局部极小点既是二值的也是可行的。针对所研究的每个问题，我们检查了现有的 QUBO 表述（若存在），识别其为何失效，并提出满足我们条件的替代松弛 QUBO。我们针对若干常见组合优化问题（包括露天开采、0-1 背包和旅行商问题的表述）展示了，这些构造允许梯度方法（如投影梯度下降和 Adam）被安全地应用以获得有效的二值解。我们的结果阐明了可微优化何时可作为二次组合目标的可靠局部求解器。

## Abstract
Many combinatorial optimization problems admit quadratic unconstrained binary formulations (QUBO) which can often be relaxed to the box $[0,1]^n$ and optimized using scalable gradient-based methods. However, the resulting non-convex landscape can often contain local optima that are spurious or infeasible. In this paper, we establish sufficient structural conditions on quadratic penalties that rule out these failures, guaranteeing that every local minimizer of the relaxed problem is both binary and feasible. For each problem we study, we examine existing QUBO formulations when available, identify why they fail when they do, and propose alternative relaxed QUBOs that satisfy our conditions. We show for several common combinatorial problems, including open-pit mining, 0--1 knapsack, and traveling salesman formulations, that these constructions allow gradient-based methods such as projected gradient descent and Adam to be safely applied to obtain valid binary solutions. Our results clarify when differentiable optimization is a reliable local solver for quadratic combinatorial objectives.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **研究问题**：组合优化问题常被转化为二次无约束二值规划（QUBO），并松弛到超立方体 \( [0,1]^n \) 上用梯度方法求解。但松弛后的非凸景观常产生**虚假的、非二值或不可行的局部极小点**，导致梯度方法不可靠。
- **核心目标**：建立**充分的结构条件**，使得松弛 QUBO 的每个局部极小点都必然是二值且可行的，从而让投影梯度下降等一阶方法能作为可靠的局部求解器。
- **背景与意义**：填补了现有离散 QUBO 设计与连续松弛优化之间的理论鸿沟，解释了为何仅靠增大惩罚系数不能保证可行性，并为可微组合优化提供了可靠性准则。

### 2. 论文提出的方法论
- **问题设定**：考虑 `^f(z)=w^Tz+γV(z), z∈[0,1]^N`，其中 `z=(x,y)` 包含核心决策变量 `x` 和辅助变量 `y`，`V` 为二次惩罚项，`γ` 为惩罚权重。
- **三个关键条件（C1–C3）**：
  - **(C1) 对角线自由**：惩罚项 `V` 在核心变量上不含 `z_i^2` 项（即 `Q_{ii}=0`）。
  - **(C2) 整数系数**：`V` 的所有系数均为整数，使得在二值点处梯度为整数。
  - **(C3) 局部可修复性**：对每个不可行的二值核心配置，存在一个可行的下降方向 `d`，使 `⟨∇V(z),d⟩ ≤ -δ`（`δ>0`），即惩罚有统一的下降量。
- **主要定理**：
  - **二值性定理**（Thm 2.5）：当 `γ > max|w_i|` 且条件 (C1)(C2) 满足时，所有局部极小点的核心变量必为二值。
  - **可行性定理**（Thm 2.7）：结合核心二值性与局部可修复性，当 `γ > L/δ`（`L` 为线性项在可行方向上的最大增幅）时，局部极小点的核心必可行。
  - **统一准则**（Thm 2.8）：若惩罚项同时满足 (C1)–(C3)，则存在有限阈值 `γ*`，当 `γ>γ*` 时所有局部极小点均为二值可行解。
- **具体问题的新构造**：
  - **露天开采**：提出“祖先惩罚”（`V_anc`），惩罚所有祖先缺失，而非仅父子关系，满足一位修复方向。
  - **0-1 背包**：对单约束使用**二值等价对角线消去** (`V_be`)，定义 `V_be = r^2 - Σ a_i x_i^2 + Σ a_i x_i`；对多约束则提出过量修正 (`V_over`) 以保证修复性。
  - **旅行商问题**：对指派松弛和时步索引 TSP 构造**对角线自由的惩罚** (`V_deg`, `V_time`)，并通过加入极小正数 ε 使所有变量在核心中获得正系数。

### 3. 实验设计
- **验证实验**：
  - **问题与数据集**：露天开采（MineLib）、0-1 背包（kplib）、旅行商（TSPLIB）。每类问题取 2 个实例。
  - **对比方案**：理论指导构造（Theory-Guided） vs. 朴素 QUBO（Naive，如直接平方惩罚或仅父子惩罚）。
  - **优化器**：无松弛变量时用投影梯度下降（PGD）；背包问题因景观病态改用投影 Adam。
  - **惩罚设置**：`γ_small = γ*+0.1`（理论阈值）和 `γ_large = 1000*γ_small`（近似无限惩罚）。
  - **评价指标**：每位运行从 10 个随机初始点出发，统计返回解的二值率（Bin%）和可行率（Feas%）。
- **最大独立集（MIS）实验**：
  - 在 Erdős–Rényi 图上测试（多种节点数 `n` 与边密度 `p`），对比 PGD 与 Gurobi、随机局部搜索（SLS）、GRASP。
  - 记录 5 分钟和 30 秒时限下的平均独立集大小，并评估 GPU 批量并行重启的效果。

### 4. 资源与算力
- 论文**未明确说明**所用的 GPU 型号、数量或训练具体时长。
- 仅在 MIS 实验提及“5-minute budget”和“batched GPU restarts”，但未给出硬件配置细节，因此算力资源无法在此总结。

### 5. 实验数量与充分性
- **数量**：对 3 个问题各取 2 个实例，共 6 组主要正确性验证实验；MIS 部分包含多种 `(n,p)` 配置（如 2000 到 40000 节点），对比多个方法，每组设置下运行 8 个图样本。
- **充分性评价**：实验作为**概念验证**是充分的——清晰展示了理论指导构造的必要性和有效性（朴素方法失败，理论构造成功），且覆盖了不同问题类型。但整体实验规模较小，更偏向于例证而非大规模基准测试；与专用离散求解器的全面比较也有限。

### 6. 论文的主要结论与发现
- 仅靠增大惩罚系数**不能消除松弛 QUBO 中的虚假局部极小点**；根本问题在于惩罚项的一阶几何结构（如梯度平坦或指向错误）。
- 所提出的三个结构条件（对角线自由、整数系数、局部可修复性）**充分保证**局部极小点的二值与可行性，且给出了明确的 `γ` 阈值。
- 该理论成功**解释了**现有 QUBO 构造（如 MIS 的边冲突惩罚）为何成功，也揭示了常见构造（如露天开采的父子惩罚）为何失败。
- 针对三个典型问题构造的新 QUBO 均能在梯度优化下可靠获得可行二值解，使 PGD/Adam 成为有效的局部求解器。

### 7. 优点
- **理论贡献突出**：给出了问题无关的、严格的局部正确性条件，而非启发式方法。
- **解释能力强**：同时覆盖了成功与失败的案例，统一了理解。
- **构造具有指导性**：提出的“祖先惩罚”“二值等价消去”“过量修正”等思路，可直接指导新问题的 QUBO 设计。
- **实验直观清晰**：用简洁的表格直接对比二值率和可行率，论证有力。

### 8. 不足与局限
- **实验覆盖面窄**：每个问题仅用 2 个实例，且未涉及大型、结构化复杂实例；结论的泛化性有待更多实证。
- **算力信息缺失**：未提供具体硬件和运行时间统计，难以评估实际计算成本。
- **只保证局部极小点**：不保证全局最优性；当存在多个局部极小点时，最终解质量依赖于初始化。
- **惩罚项约束**：假设了整数系数和核心对角线自由，对于某些自然 QUBO 形式可能不满足，需额外修正。
- **数值稳定性隐患**：理论要求 `γ` 远超线性项系数，可能导致梯度过大或病态景观，实践中虽用 Adam 缓解，但问题未根本解决。
- **模型局限**：核心定理（Thm 2.8）主要面向线性目标 + 二次惩罚；时步索引 TSP 虽有推广，但更复杂的非线性目标尚未覆盖。

（完）
