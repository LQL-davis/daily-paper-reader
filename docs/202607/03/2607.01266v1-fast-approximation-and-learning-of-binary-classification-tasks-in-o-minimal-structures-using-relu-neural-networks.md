---
title: Fast approximation and learning of binary classification tasks in o-minimal structures using ReLU neural networks
title_zh: 使用ReLU神经网络在o-极小结构中快速逼近和学习二分类任务
authors: "Clemens Kinn, Philipp Petersen"
date: 2026-06-29
pdf: "https://arxiv.org/pdf/2607.01266v1"
tags: ["query:boundary-opt"]
score: 10.0
evidence: 使用ReLU网络逼近二分类决策边界
tldr: "在o-minimal扩张的实数域中，二分类问题的决策域由可定义集刻画。本文引入可追踪集作为代理，研究ReLU神经网络对该类集合特征函数的近似能力。在连通分量有界且边界函数C^m光滑延拓的假设下，证明了L^p逼近所需网络规模为O(ε^{-p(n-1)/m})，深度与精度无关且权重多项式有界。进一步结合熵估计，对于N个均匀样本，基于合页损失的经验风险最小化分类器达到期望误差阶N^{-m/(m+pn-p)}（至多忽略任意小多项式损失）。这一工作首次为o-minimal结构的神经网络学习提供了精确的收敛速率，揭示了结构性质对逼近复杂度的影响。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.01266v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 455, \"height\": 408, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01266v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 881, \"height\": 724, \"label\": \"Figure\"}]"
motivation: 研究o-minimal可定义集分类的ReLU网络逼近理论，填补量化逼近率与统计学习率的空白。
method: 引入可追踪集，利用胞腔分解与边界C^m光滑延拓，构建ReLU网络特征函数逼近，推导网络规模上界。
result: "得到L^p逼近规模O(ε^{-p(n-1)/m})；分类器期望误差达N^{-m/(m+pn-p)}阶，深度与精度无关。"
conclusion: 为o-minimal结构神经网络学习提供首个精确收敛率，揭示结构性质对逼近复杂度的影响，奠定可定义集学习理论基础。
---

## 摘要
我们研究二分类问题，其决策集由实数域的o-极小扩张中的可定义集给出。受可定义集的胞腔分解启发，我们引入可追踪集作为可定义决策区域的经典替代，并分析其用ReLU神经网络的逼近。在连通分量数量的统一界和边界函数的适当$C^m$延拓条件下，我们证明了$[-1/2,1/2]^n$中可追踪子集的特征函数可以在$L^p$范数下被ReLU神经网络以精度$\varepsilon>0$逼近，网络规模为$\mathcal{O}(\varepsilon^{-p(n-1)/m})$，深度与$\varepsilon$无关且权重多项式有界。这为o-极小结构中某些可定义集合类建立了使用ReLU神经网络的定量逼近率。同样的方法也给出了可定义映射$[-1/2,1/2]^n \to \mathbb{R}$的一个子类的上述逼近率。然后，我们将逼近能力与ReLU神经网络类的熵估计相结合，得到使用合页损失的经验风险最小化的统计学习速率。对于$N$个均匀分布的样本，得到的分类器达到的期望误分类误差阶为$N^{-m/(m+pn-p)}$，最多相差一个任意小的多项式损失。

## Abstract
We study binary classification problems whose decision sets are given by definable sets in o-minimal expansions of the real field. Motivated by cell decomposition of definable sets, we introduce traceable sets as a classical proxy for definable decision regions and analyze their approximation by ReLU neural networks. Under uniform bounds on the number of connected components and suitable $C^m$ extensions for the boundary functions, we prove that characteristic functions of traceable subsets of $[-1/2,1/2]^n$ can be approximated in $L^p$ to accuracy $\varepsilon>0$ by ReLU neural networks of size $\mathcal{O}(\varepsilon^{-p(n-1)/m})$, with depth independent of $\varepsilon$ and polynomially bounded weights. This establishes quantitative approximation rates for certain definable collections in o-minimal structures using ReLU neural networks. The same approach also yields the stated approximation rates for a subclass of definable maps $[-1/2,1/2]^n \to \mathbb{R}$. We then combine the approximation capabilities with entropy estimates for ReLU neural network classes to obtain statistical learning rates for empirical risk minimization with hinge loss. For $N$ uniformly distributed samples, the resulting classifiers achieve expected misclassification error of order $N^{-m/(m+pn-p)}$ up to an arbitrarily small polynomial loss.

---

## 论文详细总结（自动生成）

好的，以下是根据您提供的论文内容生成的结构化中文总结。

---

### 1. 论文的核心问题与整体含义 (研究动机和背景)

-   **核心问题**：本文研究二分类问题，其决策边界由具有特定数学结构（o-极小结构中的可定义集）的集合所定义。核心目标是为这类问题建立使用ReLU神经网络的**定量逼近理论**和**统计学习理论**。
-   **研究动机**：
    -   深度神经网络在实践中取得了巨大成功，但其对特定类型数据的逼近能力和学习效率的理论基础仍在发展中。
    -   o-极小结构包含了许多行为良好的集合（如半代数集），具有有限的连通分量数和VC维，是进行学习理论分析的理想候选框架。
    -   现有的分类问题理论工作通常依赖于决策边界的平滑性或属于Barron类函数等假设。本文提供了一个基于“温驯几何”和模型论的新框架。
-   **整体含义**：本文试图在o-极小结构这一更广阔的数学框架下，统一逼近理论和统计学习理论，为实践中遇到的诸多决策集提供强大的逼近和估计速率保证。

### 2. 论文提出的方法论 (核心思想、关键技术细节)

-   **核心思想**：受o-极小结构中胞腔分解定理的启发，将复杂的可定义集分解为具有特定“矩形”形状的简单构建块，并分析ReLU网络对这些构建块的逼近能力。
-   **关键技术细节：可追踪集**
    -   论文引入了**可追踪集**作为核心分析对象。一个集合被称为m-可追踪的，如果它可以递归地定义为一系列“开口盒子/函数图”的拼接，其边界由一系列对有界的$C^m$光滑函数$f_i, g_i$所界定。
    -   **可延拓性**：为获得有效的逼近，要求这些边界函数$f_i, g_i$的某种包络能够$C^m$光滑地延拓到整个超立方体上，这被称为$(m, B)$-**延拓性质**。所有满足此性质的$(m, \ell)$-可追踪子集的族记为$K^{B,\ell}_{m,n}$。
-   **技术流程与公式**:
    1.  **分解**：将目标集合$\Omega$分解为有限个不相交的可追踪集。
    2.  **边界函数逼近**：集合的特征函数$\chi_\Omega$可以表示为一系列与边界函数相关的**水平集函数**的最大值和最小值组合。例如，对于$A \in K^{B}_{m,n}$，其特征函数$\chi_A$本质上是其封闭包$\text{cl}(A)$的特征函数，可以表示为：
        $$\chi_{\text{cl}(A)} = \min_{i \in [n]}\{\mathcal{F}_i, \mathcal{G}_i\}$$
        其中$\mathcal{F}_i, \mathcal{G}_i$是由边界函数的延拓定义的水平集函数，如$\mathcal{F}_i(x) = H_i(x_i - F_i(x_{<i}))$。
    3.  **ReLU网络逼近**：
        -   利用已有结果，用ReLU网络逼近单个水平集函数$\mathcal{F}_i, \mathcal{G}_i$。
        -   利用ReLU网络实现**最小值**和**最大值**运算的功能模块。
        -   通过网络组合和并行化技术，将逼近水平集函数的网络与最小值/最大值网络连接起来，构建最终的逼近网络$\Phi^{\Omega}_{\varepsilon}$。
    4.  **复杂度控制**：通过精心控制各网络模块的大小和深度，最终网络以$L^p$误差$\varepsilon$逼近$\chi_\Omega$所需的**规模**为$\mathcal{O}(\varepsilon^{-p(n-1)/m})$，而**深度**与$\varepsilon$无关。

### 3. 实验设计

-   **实验场景与数据集**：**本文是纯理论性论文，没有进行传统的数值实验或使用具体的数据集**。
-   **Benchmark与对比方法**：
    -   论文的“benchmark”是其设定的理论最优逼近率和学习率。
    -   其理论结果与现有关于Barron类边界和$C^m$光滑边界的分类问题的理论工作进行对比，指出其框架的普适性（o-极小结构包含更广的集合类）。
    -   在学习理论部分，将结果与[PV21]等基于熵估计的工作建立了联系和对比。

### 4. 资源与算力

-   **算力使用情况**：**论文中未提及任何计算资源的使用**。由于这是一项严格的理论与数学证明工作，不涉及模型训练或GPU计算，因此没有相关的算力、GPU型号或训练时长信息。

### 5. 实验数量与充分性

-   **实验性质**：本文**不包含数值实验**。
-   **充分性与客观性**：论文的验证完全依赖于严格的**数学推导和证明**，而非实验。其“充分性”体现在理论框架的完整性上：从设定（可追踪集、延拓性质）出发，推导出逼近定理，进而基于熵估计得到学习率上界，构成了一个自洽的理论闭环。这种方式在客观性上是顶级的，因为它不依赖于数据、随机种子的选择和工程实现，结论具有绝对的确定性。

### 6. 论文的主要结论与发现

-   **逼近定理**：对于$n \ge 2$，边界函数$C^m$光滑延拓的$\ell$-可追踪集族$K^{B,\ell}_{m,n}$，其特征函数在$L^p$范数下可以被ReLU网络逼近至任意精度$\varepsilon$，网络规模为$\mathcal{O}(\varepsilon^{-p(n-1)/m})$，深度仅取决于$m, n, \ell$，权重最大值为$\varepsilon$的多项式级。
-   **学习率定理**：对于从均匀分布中抽取的$N$个独立样本，使用一个特定架构（规模为$\mathcal{O}(N^{p(n-1)/(m+pn-p)})$）的ReLU网络进行基于合页损失的经验风险最小化，期望误分类误差可达$N^{-m/(m+pn-p)}$阶，直至一个任意小的多项式因子。
-   **框架的普适性**：通过可追踪集这一概念，成功地为o-极小结构中更广泛的可定义集合类（包括半代数集和许多其他类型的集合）建立了定量的逼近和学习理论。

### 7. 优点

-   **理论创新性强**：首次在模型论与深度学习的交叉领域给出了精确的逼近率和学习率，而非常规的定性PAC可学习性分析。
-   **框架统一且强大**：通过引入“可追踪集”和“延拓性质”，将复杂的几何结构（o-极小可定义集）与已有的神经网络逼近工具箱（如对Barron类函数的逼近）巧妙地联系起来。
-   **结果边界清晰**：对网络复杂度（规模、深度、权重）与逼近精度之间的关系给出了显式的、依赖关键参数$m, n, p$的定量边界。
-   **深度与精度无关**：理论结果证明，达到更高精度只需增加宽度，而深度可以保持恒定，这对网络设计具有指导意义。

### 8. 不足与局限

-   **缺乏实证验证**：作为一篇纯理论论文，没有任何数值实验来验证所提出的逼近率和学习率的实际效果，其理论常数的紧致性和实用性未知。
-   **假设条件较强**：核心假设是决策边界的函数具有全局的$(m, B)$-延拓性质，这要求边界函数在整个定义域上具有一致有界的光滑性，实践中可能难以满足或验证。
-   **应用限制**：理论直接应用于o-极小结构中的可定义集合，虽然这类集合很广，但判断一个实际分类问题的决策边界是否属于某个特定的o-极小可定义集合并非易事。
-   **指数依赖问题**：逼近规模$\mathcal{O}(\varepsilon^{-p(n-1)/m})$存在“维数灾难”问题，规模随着输入维度$n$的提升呈指数增长，这是高维逼近中的通病，本文也未能避免。

（完）
