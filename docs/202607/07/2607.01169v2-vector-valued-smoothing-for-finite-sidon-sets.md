---
title: Vector-valued smoothing for finite Sidon sets
title_zh: 有限Sidon集的向量值光滑化
authors: "Jianfeng Hou, Hongbin Zhao"
date: 2026-07-01
pdf: "https://arxiv.org/pdf/2607.01169v2"
tags: ["query:boundary-opt"]
score: 10.0
evidence: 通过凸二次规划进行边界优化应用于Sidon集
tldr: "有限Sidon集的最大基数估计是加法组合中的经典问题，其精确上界的研究有助于理解整数集的结构。本文采用向量值平滑技术，通过多个平滑核合作构造边界主控函数，并利用其L^2能量平均，将优化问题转化为严格凸二次规划，得到显式对偶。通过八核数值候选转化为有限有理证书，并用纯精确算术验证，证明了F(N)≤sqrt(N)+0.9435N^{1/4}+O(1)。这一结果改进了此前上界中的常数因子，并展示了数值优化与严格证明相结合的新范式。"
source: arxiv
selection_source: fresh_fetch
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.01169v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1303, \"height\": 617, \"label\": \"Table\"}]"
motivation: 寻求更精确的有限Sidon集基数上界，以改进经典加法组合问题的已知结果。
method: 向量值卷积不等式，多平滑核协同构造主控函数，L^2能量平均，转化为凸二次规划，数值解有理证书精确验证。
result: "证明F(N) ≤ sqrt(N) + 0.9435 N^{1/4} + O(1)，改进了上界常数因子。"
conclusion: 得到更紧的渐近上界，并提供了一种数值优化与严格证明相结合的新方法。
---

## 摘要
设 $F(N)$ 表示 $\{0,1,\ldots,N-1\}$ 的Sidon子集的最大基数。我们证明了 \[ F(N)\le N^{1/2}+0.9435N^{1/4}+O(1)。\] 论证使用了一个向量值卷积不等式，其中多个光滑化核共同作用产生一个边界控制函数，同时它们的 $L^2$ 能量被平均。对于固定的核与混合权重，边界优化是一个严格凸二次规划，具有显式对偶形式。一个包含八个核的数值候选被转换为一个有限有理证书，并仅使用精确算术进行验证。

## Abstract
Let $F(N)$ denote the largest cardinality of a Sidon subset of $\{0,1,\ldots,N-1\}$. We prove \[ F(N)\le N^{1/2}+0.9435N^{1/4}+O(1). \] The argument uses a vector-valued convolution inequality in which several smoothing kernels cooperate to produce a boundary majorant while their $L^2$ energies are averaged. For fixed kernels and mixing weights, the boundary optimization is a strictly convex quadratic program with an explicit dual. An eight-kernel numerical candidate is converted into a finite rational certificate and verified using exact arithmetic only.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：确定有限Sidon集的最大基数 $F(N)$ 的更精确上界。Sidon集（也称 $B_2$ 集）是指集合中所有非零差均不同的整数子集，它在加法组合与编码理论中具有基础地位。
- **研究动机**：经典结果给出 $F(N) \le N^{1/2} + N^{1/4} + O(1)$，尔后二项系数 $N^{1/4}$ 的常数逐步被改进至 $0.98183$，最近计算机辅助的工作将其降至 $0.97633$。本文旨在进一步降低该常数，逼近可能的极限。
- **整体含义**：作者将上界常数推进到 $0.9435$，并提供了一种“数值搜索 + 有理证书严格验证”的证明范式，即利用数值优化寻找候选参数，最终通过精确有理算术完成不依赖数值容差的严格数学证明。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：**向量值光滑化**。不再对单一的平滑核进行边界控制，而是在一个加权的 Hilbert 直和空间中放置多个平滑核（此处为8个），仅要求它们的加权组合满足边界覆盖不等式，同时平均各核的 $L^2$ 能量。这使得一个核的“低对角能量”可被另一个“更好适应边界”的核所补偿，获得比单独标量界经平均更优的整体界。
- **关键技术细节**：
  - **多核光滑引理（Lemma 2.1）**：给定对称概率核 $p^{(r)}$、混合权重 $\lambda_r$ 以及边界权向量 $w^{(r)}$，若它们满足覆盖条件 $\sum_r \lambda_r \sum_i p_i^{(r)} w^{(r)}_{q+i} \ge 1$（对 $0\le q\le Lm$），则可定义能量参数 $a$ 和 $b$，并证明 $F(N) \le N^{1/2} + \sqrt{ab} N^{1/4} + O(1)$。
  - **转化为凸二次规划**：固定核和混合权重后，最小化 $\sqrt{ab}$ 等价于求解一个严格凸二次规划：$\min\{ w^T D w : A w \ge c \}$，其对偶为 $\max_{y\ge 0} \{c^T y - \frac14 y^T G y\}$（$G = A D^{-1} A^T$）。利用互补松弛条件可获得唯一最优边界向量。
  - **数值搜索与有理化**：使用交替优化（固定核权重时解凸规划，固定边界时优化核与权重）探索参数空间（$R=8, m=32, L=4$）。将最终浮点候选四舍五入为有理数，并对边界向量施加公共有理修正以严格满足所有覆盖不等式，最终得到一个完全由有理数构成的证书。
  - **严格验证**：仅使用 Python 标准库进行精确有理数算术检查，验证所有 129 个覆盖不等式，并计算精确的 $a, b$ 及 $ab < (0.9435)^2$，从而完成不依赖数值误差的证明。

### 3. 实验设计：使用了哪些数据集/场景，它的 benchmark 是什么，对比了哪些方法

- **场景与数据集**：本工作为纯数学证明，无传统意义上的数据集。数值部分属于参数搜索，不涉及外部数据集。
- **Benchmark**：对比的对象是历史上逐步改进的 $N^{1/4}$ 项系数：
  - Erdős–Turán (1941): $1$
  - Balogh et al. (2023): $0.998$
  - O’Bryant (2025): $0.99703$
  - Carter–Hunter–O’Bryant (2025): $0.98183$
  - Tao et al. (2026): $0.97633$（计算机辅助）
- **对比方法**：论文仅与上述历史结果比较最终常数，未在实验节内与方法直接比较。在搜索过程中，作者展示了在同一参数族下，增加核的灵活性如何逐步降低 $\sqrt{ab}$ 值（从单核 $0.9461$ 逐次降至八核 $0.94349259$ 的浮点值和后续有理证书值）。

### 4. 资源与算力：如果文中有提到，请总结使用了多少算力

- **论文未明确说明**所用 GPU 型号、数量或训练时长。
- 文中提及提供了“探索性搜索脚本 `sidon_numerical_search.py`”，表明数值优化过程使用了常规计算机，但未给出具体算力资源细节。验证部分（`sidon_certificate_8kernel.py`）仅需 Python 标准库，计算量极小。

### 5. 实验数量与充分性：大概做了多少组实验，这些实验是否充分、客观、公平

- **实验数目**：主要搜索实验在 $m=32, L=4$ 下进行，从 $R=1$ 到 $R=8$ 逐步增加核的数量和对称模式，共得到 8 个搜索类别（见表1）。每个类别均给出了找到的最佳 $\sqrt{ab}$ 值。
- **充分性**：搜索问题是非凸的，表中所列为“当前搜索类下找到的最佳值”，并非全局最优的证明。但论文最终证明并不依赖搜索的全局最优性：仅需提供一个满足所有约束的**有理证书**，验证后即确立 $0.9435$ 的上界。因此实验只需找到一个充分好的可行点，而非严格证明其最优性。从这个角度看，实验是充分的。
- **客观性与公平性**：所有比较均基于精确算术验证，有理证书的检查是完全客观的。数值搜索过程中使用的基准（标量光滑化或较少核的系统）为公平的同框架消融。

### 6. 论文的主要结论与发现

- **主要结论**：证明了 $F(N) \le N^{1/2} + 0.9435 N^{1/4} + O(1)$，给出了一个显式常数 $\gamma_0 = 0.943492590\dots < 0.9435$，这是目前已知的最好上界。
- **关键发现**：向量值光滑化方法能够有效结合多个核的优势，使边界控制与能量最小化得以解耦，从而在不显著增加证明复杂度的情况下改进常数。并且，通过将数值搜索结果转化为严格的有理验证，可以架起计算机辅助搜索与纯数论证明之间的桥梁。

### 7. 优点：方法或实验设计上有哪些亮点

- **方法创新**：首次在Sidon集上界中引入多核向量值光滑化，突破了传统单核方法的限制。
- **解耦与凸优化**：固定核后，边界问题被精确转化为严格凸二次规划并拥有对偶形式，使问题可高效求解且解唯一。
- **可信验证**：最终证明完全不依赖浮点计算，仅通过有理数精确算术完成，消除了数值误差的担忧，兼具计算机搜索的效率和传统证明的严密性。
- **开源与可复现**：所有证书和验证脚本均在 GitHub 上公开，SHA-256 哈希确保了验证器的完整性。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

- **优化最优性未证**：最终的参数组合（核、权重）是搜索发现的一个局部解或可行解，并未证明它在给定多核函数类中是全局最优。因此常数 $0.9435$ 仍有进一步改进的空间。
- **参数选择的经验性**：核数 $R=8$、$m=32$、$L=4$ 这些超参数的选择基于试探，未系统说明为何这些取值是充足的，可能在其他参数配置下还能获得更优界。
- **方法的可推广性**：所述方法针对Sidon集的 $L^2$ 能量估计设计，要推广到其他加法组合问题，可能需要新的边界控制器设计和相关性正性条件，文中提及若引入交叉核项需附加“相关正性”约束，增加了复杂度。
- **实验细节缺失**：未披露数值搜索所耗时间、具体优化器及超参数，使得数值搜索过程难以直接复现（尽管最终证明可复现）。

（完）
