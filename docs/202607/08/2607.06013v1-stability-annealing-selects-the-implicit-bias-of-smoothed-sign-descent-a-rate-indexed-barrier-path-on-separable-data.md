---
title: "Stability Annealing Selects the Implicit Bias of Smoothed Sign Descent: A Rate-Indexed Barrier Path on Separable Data"
title_zh: 稳定性退火选择平滑符号下降的隐式偏差：可分数据上的率索引障碍路径
authors: "Xiangwu Wang, Chengwei Cao, Yicheng Song, Ran Bi, Peilin Yu"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.06013v1"
tags: ["query:boundary-opt"]
score: 9.0
evidence: 通过平滑符号下降的隐式偏差优化决策边界
tldr: 在可分离数据的线性分类中，自适应梯度方法的隐式偏差可能因数值稳定性常数而改变。本文针对带加权指数损失的平滑符号下降，提出速率控制的稳定性退火策略，证明归一化迭代收敛到凸Burg型障碍函数的极小化点。证明通过将原动力学转化为对偶空间上的熵镜像上升，利用KL散度递归控制对偶间隙，并导出显式渐近包络。实验精确验证对偶恒等式，展示预测路径和速率图，并观察到固定ε交叉的累积时间缩放规律，划定了理论适用范围。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.06013v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 837, \"height\": 695, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06013v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1794, \"height\": 1169, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06013v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1585, \"height\": 527, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06013v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1596, \"height\": 531, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06013v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1727, \"height\": 1180, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06013v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1724, \"height\": 767, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06013v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1732, \"height\": 1169, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06013v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1720, \"height\": 1191, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06013v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1741, \"height\": 594, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.06013v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 861, \"height\": 315, \"label\": \"Table\"}]"
motivation: 稳定性退火如何影响平滑符号下降的隐式偏差及其收敛路径亟待理论刻画。
method: 对可分离数据全批量线性分类，将稳定性退火平滑符号下降转化为凹对偶目标上的熵镜像上升，并利用KL递归控制对偶间隙。
result: "证明归一化迭代收敛到凸Burg型障碍极小化，得到显式S_t^{-1/2}包络，并完整刻画静态障碍几何；实验验证对偶恒等式至浮点误差。"
conclusion: 该工作阐明了速率退火平滑符号下降的隐式偏差理论，提出速率索引的障碍路径，为理解自适应优化器偏差提供新工具。
---

## 摘要
自适应梯度方法可能偏爱与梯度下降不同的最大间隔分离器，然而一个固定的正数值稳定常数最终又会改变更新几何。本文研究了可分数据上全批量线性分类的速率控制中间情形。对于具有加权指数损失的无记忆稳定性退火平滑符号下降，我们证明归一化迭代收敛到一个凸Burg型障碍在间隔切片上的最小值点。证明将动力学精确重写为凹对偶目标上的熵镜像上升，通过KL递归控制对偶间隙，并产生显式的 S_t^{-1/2} 归一化迭代包络。静态障碍几何被完整刻画，包括KKT条件和两个端点极限。实验验证了精确的对偶恒等式至浮点误差，展示了预测的路径和速率图，并显示了累积时间中经验固定ε交叉标度。我们进一步报告了对逻辑尾部、固定ε交叉和自适应方法变体的鲁棒性和边界诊断，描绘了所证平滑符号理论的适用范围。

## Abstract
Adaptive gradient methods can favor max-margin separators that differ from gradient descent, yet a fixed positive numerical stability constant eventually changes the update geometry again. This paper studies the rate-controlled middle case for full-batch linear classification on separable data. For memoryless stability-annealed smoothed-sign descent with weighted exponential loss, we prove that the normalized iterates converge to the minimizer of a convex Burg-type barrier over a margin slice. The proof rewrites the dynamics exactly as entropic mirror ascent on a concave dual objective, controls the dual gap by a KL recursion, and yields an explicit S_t^{-1/2} normalized-iterate envelope. The static barrier geometry is fully characterized, including KKT conditions and both endpoint limits. Experiments validate the exact dual identities to floating-point error, illustrate the predicted path and rate diagram, and show an empirical fixed-epsilon crossover scaling in cumulative time. We further report robustness and boundary diagnostics for logistic tails, fixed-epsilon crossover, and adaptive-method variants, delineating the scope of the proved smoothed-sign theory.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义
- **研究动机**：梯度下降在可分数据上会隐式地收敛到ℓ₂最大间隔分离器，而自适应梯度方法（如Adam）的隐式偏差却可能指向ℓ∞型间隔。其中数值稳定性常数ε起关键作用：ε固定且不可忽略时，小梯度会重新退化为类似梯度下降的方向；ε可忽略时则接近符号下降。这些端点之间的“中间”区域缺少严格理论。
- **核心问题**：如果稳定性常数ε以受控的指数速率 `ε_t = ε₀ exp(−κ S_t)` 退火，那么无记忆平滑符号下降（smoothed sign descent）最终会选择怎样的分离器？本文证明这个选择由一个**速率索引的凸障碍路径**唯一确定。
- **整体含义**：退火速率κ直接转化为间隔约束，平滑映射在坐标方向上的竞争会诱导出Burg型障碍，从而给出可预测、可解释的优化器隐式偏差路径。这为理解自适应算法中ε衰减的效应提供了精确的理论机制。

### 2. 方法论
- **核心思想**：将稳定性退火的平滑符号动力学等价地转化为**凹对偶目标上的熵镜像上升**，利用KL散度的递归收紧过程控制收敛，最终将归一化迭代刻画为**一个凸Burg障碍在间隔切片上的极小化点**。
- **关键公式与组件**：
  - **原问题设定**：线性分类，加权指数损失 `L(w) = Σ a_i exp(−z_i^⊤ w)`，更新律
    ```
    w_{t+1} = w_t + η_t * ( −∇L(w_t) ) / ( |∇L(w_t)| + ε_t ),   ε_t = ε₀ exp(−κ S_t)
    ```
    其中 `S_t = Σ η_s` 表示累积学习率时间，绝对值与除法按坐标逐元素进行。
  - **静态障碍程序**：定义 Burg 型障碍 `B(u) = Σ_j [−|u_j| − log(1−|u_j|)]`，则速率κ对应的静态分离器为
    ```
    u_κ = arg min_{u∈(−1,1)^d} B(u)   s.t.  z_i^⊤ u ≥ κ  ∀i
    ```
    该程序有唯一解，且满足 KKT 条件。
  - **对偶视角与动态证明思路**：
    - 引入缩放势 `Φ(u)=ε₀ B(u)` 及其共轭 `Φ^*(q)`，构建凹对偶目标 `D(λ) = κ·1^⊤λ − Φ^*(Z^⊤λ)`。
    - 定义精确对偶变量 `λ_{i,t} = a_i exp(κ S_t − z_i^⊤ w_t)`，得到等价动力学：
      ```
      λ_{t+1} = λ_t ⊙ exp(η_t ∇D(λ_t))           （熵镜像上升）
      ```
    - 固定任一对偶最优解 λ^⋆，定义 KL-like 势 `V_t` 和对偶间隙 `Δ_t = D^⋆ − D(λ_t)`。利用一步递归恒等式与凹性可得
      ```
      V_{t+1} − V_t ≤ −η_t Δ_t + (e/2) η_t^2 G^2 ‖λ_t‖₁
      ```
    - 在 `∑ η_t = ∞`、`∑ η_t² < ∞` 及步长上界条件下，通过离散 Gronwall 论证得到 `∑ η_s Δ_s < ∞`，并由障碍函数的强凸性推出 `‖d_t − u_κ‖² ≤ C_Φ Δ_t`，最终用 Jensen 不等式导出归一化迭代的显式包络：
      ```
      ‖w_t/S_t − u_κ‖₂ ≤ ‖w_0‖₂/S_t + sqrt(C_Φ C_D / S_t)
      ```
    - 再利用 spike-exclusion 技术将对偶间隙的点态衰减 `Δ_t→0` 从平均控制中分离出来，得到方向收敛 `w_t/S_t → u_κ` 和重标度梯度收敛 `e^{κ S_t}(−∇L(w_t)) → q_κ`。
- **端点行为**：当 κ↓0 时 `u_κ/κ → v_2`（ℓ₂硬间隔分离器）；当 κ↑γ_∞ 时 `u_κ` 收敛到 ℓ∞ 最大间隔集合。

### 3. 实验设计
- **数据集**：采用三类合成线性可分数据，覆盖不同的几何与条件数：
  - 各向同性随机可分数据；
  - 控制支持向量数量的数据；
  - 相关、病态条件数据。
  - 所有数据满足 `d ∈ {10, 25, 50, 100}`，样本数 `n = 4d`，每个配置使用 20 个随机种子。
- **基准与对比方法**：
  - 主要验证对象是 **定理预测的静态障碍解 `u_κ`**，通过凸规划求解器获得参考解。
  - 对比的优化器包括：**SGD、RMSProp、Adam（带/无偏置校正）** 等，但这些仅作为 **边界诊断**，检验从平滑符号下降向自适应方法迁移的可能性，且论文声明这些不构成新的定理。
- **评估指标**：
  - 归一化迭代误差 `Epc = ‖w_t/S_t − u_κ‖₂/√d` 和方向误差；
  - 对偶恒等式残差（如 `e^{κ S_t}(−∇L(w_t)) − Z^⊤λ_t`）验证动力学等价性到浮点精度；
  - 重标度梯度误差、间隔误差 `|γ(w_t/S_t) − κ|`；
  - 固定 ε 交叉的标度律拟合 `S_{τ_ε} = b₀ + b₁ log(1/ε)` 及斜率比 `ρ = b₁ γ_∞`。
- **参数设置**：指数损失权重包含均匀及对数均匀正权重；学习率使用 `η_t = η₀ (t+t₀)^{−0.75}`（以及 0.60、0.90 等消融）；退火速率 `κ/γ_∞ ∈ {0.10, 0.25, 0.50, 0.75, 0.90}`；`η₀` 满足 `η_t G ≤ 0.5`。实验使用 float64 精度。

### 4. 资源与算力
- 论文**未明确说明**使用何种 GPU、数量或训练时长。
- 实验仅涉及合成线性数据、全批量梯度更新，维度最高 100，总轨迹数约数千条，计算规模较小，推断主要基于 CPU 即可完成，无需大型算力集群。

### 5. 实验数量与充分性
- **实验数量**：
  - 主定理验证套件：3200 条动态轨迹，其中 2716 个成功获得障碍求解器的配置用于聚合统计；另有长时域展示 89 个成功配置。
  - 固定 ε 交叉实验覆盖 3 种学习率衰减指数、3 个数据族、20 个种子、ε 从 10⁻² 到 10⁻¹²、阈值 c ∈ {0.5, 1, 2}，统计了大量过渡记录。
  - 鲁棒性诊断包括逻辑损失、不同 ε₀、不同支持向量数量、高维、边界病理等 12–18 种配置组合，未在预设网格内发现任何数值反例。
- **充分性与公平性**：
  - 实验设计了消融（衰减率、ε₀、数据族、维度等）和对比优化器，对主定理的预测误差、对偶残差均给出分布而非单一种子，避免了选择偏差。
  - 对比自适应方法时明确指出未满足转移条件，未强行宣称定理拓展，保持科学客观。
  - 但实验**完全局限于合成可分线性分类**，没有真实数据或非线性模型验证，因此对该理论在实际复杂场景的泛化性信息有限。

### 6. 主要结论与发现
- **理论结论**：在带加权指数损失、稳定性作 `ε_t = ε₀ e^{−κ S_t}` 退火的无记忆平滑符号下降中，归一化迭代 `w_t/S_t` 必然收敛到唯一 Burg 障碍在间隔切片上的极小化点 `u_κ`，且收敛速率具备 `O(1/√S_t)` 的量级。该结果给出了一条**由速率 κ 索引的隐式偏差路径**，连接了 ℓ₂ 最大间隔（κ→0）和 ℓ∞ 最大间隔（κ→γ_∞）。
- **实验发现**：
  - 精确对偶等式残差低至浮点误差量级（∼10⁻¹⁶~10⁻¹³），直接验证了熵镜像上升表达式的正确性。
  - 观测到的间隔 `γ(w_t/S_t)/γ_∞` 随检查点移动趋近于预测的 κ/γ_∞ 线。
  - 固定 ε 的交叉时间标度律 `S_{τ_ε} = γ_∞^{-1} log(1/ε) + O(1)` 在经验上成立，斜率回归系数 ρ 接近 1。
  - Adam/RMSProp 的加权转移残差较大，无法由平滑符号结果直接继承，支持了该方法与平滑符号之间的几何差异。

### 7. 优点
- **理论贡献明确且新颖**：首次为稳定性退火的平滑符号下降建立了**精确的率索引隐式偏差定理**，将退火速率与 Burg 障碍及间隔切片直接联系起来，填补了梯度下降和自适应方法端点之间的理论空白。
- **证明技巧优雅**：通过变量代换将对偶动力学写为**熵镜像上升**，并利用 KL 稳定递归避免了对主动集、严格互补性、归一化迭代有界性等常见强假设，具有较强普适性。
- **实验验证高度严谨**：对对偶代数恒等式进行了浮点级残差验证，展示误差分布而非单点表现；固定 ε 交叉给出一致标度；大量消融与边界诊断清晰地划定了定理适用范围，做到了“声称什么就验证什么”。
- **可视化与解释性强**：通过二维路径图、率图、机理诊断（退火时钟对比、ε₀ 时移坍缩）帮助理解障碍路径的几何直觉。

### 8. 不足与局限
- **模型与数据假设严格**：
  - 限于**线性模型**和**全批量梯度**，无法直接扩展到深度神经网络或 mini-batch 随机梯度。
  - 损失函数为**加权指数损失**，逻辑损失仅作为经验鲁棒性检验，无对应收敛定理。
  - 数据必须**线性可分**且满足 ℓ∞ 最大间隔 `γ_∞ > 0`。
- **速率区间受限**：定理仅覆盖 `0 < κ < γ_∞` 的开区间，端点 κ=0、κ=γ_∞ 以及超出该范围的速率不包含在内，后者对应不同的极限行为或发散情况。
- **自适应方法迁移未成功**：Adam、RMSProp 等流行的自适应优化器在相同设定下不能满足转移条件，表明本理论与实践中自适应算法的偏差之间存在间隙，不能直接套用。
- **固定 ε 交叉停留于经验**：尽管观测到清晰的标度律，但缺乏均匀的双侧梯度尾部界，因此交叉标度尚未被严格证明。
- **实验局限性**：所有实验均在低维合成数据上进行，无真实世界数据集，无法判断在特征噪声、类别不平衡或高维稀疏场景下的行为。

（完）
